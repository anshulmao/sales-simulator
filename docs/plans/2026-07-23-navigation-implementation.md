# Navigation Completion Implementation Plan

1. Add a reusable `BackLink` component for focused-flow screens.
2. Add top-level back links to setup and onboarding.
3. Add state-aware exit navigation to the call screen.
4. Add an accessible confirmation dialog and active-call unload protection.
5. Verify formatting, TypeScript compilation, production build, and route
   destinations.
6. Commit the design and implementation together, then push `main` to `origin`.
